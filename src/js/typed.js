import Typed from 'typed.js';

var options = {
  strings: ['Filmoteka'],
  typeSpeed: 80,
  showCursor: false,
  contentType: 'text',
  loopCount: 5,
};

var typed = new Typed('.header__logo', options);
