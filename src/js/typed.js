import Typed from 'typed.js';

let options = {
  strings: ['Filmoteka'],
  typeSpeed: 80,
  showCursor: false,
  contentType: 'text',
  loopCount: 5,
};

let typed = new Typed('.header__logo', options);
